# Các tệp và thư mục được tạo ra khi build - bỏ qua định dạng
dist
dist-ssr
build
coverage

# Thư mục và tệp phụ thuộc - bỏ qua định dạng
node_modules
.pnp
.pnp.js

# Tệp nhật ký - bỏ qua định dạng
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Các tệp được tạo tự động - bỏ qua định dạng
*.local
*.tsbuildinfo
next-env.d.ts

# Tệp khóa phụ thuộc - bỏ qua định dạng để tránh xung đột
package-lock.json
yarn.lock
pnpm-lock.yaml

# Các tệp khác cần bỏ qua định dạng
.DS_Store
.cache
.env*
!.env.example